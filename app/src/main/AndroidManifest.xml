<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="sk.paraska.android.ebookreader">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/logo_owl_book"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:hardwareAccelerated="true">
        <activity android:name="sk.paraska.android.ebookreader.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <provider
            android:name="sk.paraska.android.ebookreader.provider.EBooksContentProvider"
            android:authorities="sk.paraska.android.ebookreader.provider.EBooksContentProvider"
            android:enabled="true"
            android:exported="false" />

        <activity android:name="sk.paraska.android.ebookreader.PagerEBookActivity" />
        <activity android:name="sk.paraska.android.ebookreader.EBookDetailActivity"></activity>
    </application>

</manifest>
